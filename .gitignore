.planning/

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# PyInstaller build artifacts (the .spec is checked in; dist/build are not)
dist/
build/
*.exe

# Installer output (the .iss is checked in; compiled Setup is not)
installer/Output/

# Secrets / local env — NEVER commit credentials, tokens, or runtime logs
.env
.env.*
*.log
*.pem
*.key
credentials.json
*-credentials.json
